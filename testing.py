my_list = ['abhishek', 'admin', 'user', 'google', 'maths']

new_users = ['ABHISHEK', 'the', 'user', 'gssdf', 'science']

for users in my_list:
	if users.lower() in new_users:

		print("Username " +users+" has already taekn"), m